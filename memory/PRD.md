# 127 — Your online IT Library — PRD

**Language:** Communicate with the user in **Dutch (NL)**.

## Original problem statement
Dutch IT-tools web app ("127"). Suite of browser-based hardware/diagnostic tools + an "Autosoft" admin panel. Stack: React + FastAPI + MongoDB. Domain: **127.be**. Pexels images/videos are proxied through the backend (API key kept server-side).

## Core tools (public routes)
- `/dpd` Dead Pixel Detector
- `/printer` Printer Tester
- `/sscreen` Screen Refresh Tester
- `/wea` Webcam & Audio Tester
- `/password` Password Generator
- Admin panel on protected routes (JWT + TOTP 2FA). Admin: `admin@127.be`.

## Key architecture
- Backend `/app/backend/server.py` (single file, ~1375 lines). Routes prefixed `/api`.
- Pexels proxy: `/api/pexels/photos`, `/api/pexels/videos` (in-memory TTL cache, graceful empty fallback).
- Tool toggle: `/api/tools`, `/api/tools/{id}/status` (fail-open: unknown id → enabled:true).
- Frontend env: `REACT_APP_BACKEND_URL`. Backend env keys: MONGO_URL, DB_NAME, CORS_ORIGINS, JWT_SECRET_KEY, ADMIN_EMAIL, TOTP_SECRET, PEXELS_API_KEY.

## Implemented (log)
- 2026-06: Homepage + Printer Tester, Password Generator en Dead Pixel Detector omgezet naar dezelfde solid dark-mode als de toolbox (#0b0f19). Pexels-achtergrond-fetch verwijderd uit die tools; homepage gradient vervangen door solid dark. DPD-intro is nu dark; de fullscreen kleurentest behoudt de testkleuren. (Webcam & Audio Tester nog niet omgezet — niet in de opdracht vermeld.)
- 2026-06: Homepage "Toolbox" button — removed arrow, text now just "Toolbox", black background (no glow), sized like the other buttons. Toolbox switched from animated gradient to solid dark mode (#0b0f19). Removed the loading screen from PrinterTest, WebcamAudioTest, PixelTest and PasswordGenerator (isLoading starts false) so they open directly. Verified via screenshots. 127 logo made responsive (h-8 mobile / h-12 desktop).
- 2026-06: Restyled the Developer Toolbox to match the 127 homepage look — same animated multi-color gradient background (`.animated-gradient-bg`) with dark overlay, glass-morphism panels/cards/sidebar/top-bar, white text, and the 127 logo in the header. Toolbox now runs in a fixed dark-gradient aesthetic (theme toggle removed from the toolbox header to match the single homepage look). Landing page "Open the Developer Toolbox" button made prominent (blue→purple gradient, glow, wrench icon). Verified via screenshots (home, landing, tool pages readable). Tool functionality unchanged (see iteration_4).
- 2026-06: **Tool127 Developer Toolbox MVP** added at `/tools` (+ `/tools/:toolId`). 31 client-side tools across 8 categories (JSON, Security & Encoding, Text, Formatter, Developer, Web, Design, Network). Features: command palette (Cmd+K), global + home search, favorites & recents (localStorage), dark/light theme toggle (scoped `.dark`, default dark), copy/download, validation. Modular registry at `src/tools/registry.js`; layout `src/layouts/ToolboxLayout.jsx`; tools in `src/tools/<category>/*.jsx`. Libs: js-yaml, fast-xml-parser, papaparse, crypto-js, sql-formatter, js-beautify, ua-parser-js, cronstrue. DNS via Google DoH, WHOIS via RDAP (browser fetch). Landing has `open-toolbox-link` entry. Verified iteration_4.json — frontend 100%, all tools ground-truth checked (SHA-256, JWT, IP calc), DNS/WHOIS live. Existing site/admin untouched.
- 2026-06 (fork): **English-only overhaul + cleanup.** Removed the `t('nl','en')` translation helper from all components (script-based, quote-safe) and hardcoded English. Deleted obsolete files: `contexts/LanguageContext.js`, `components/LanguageToggle.jsx`, `utils/translations.js`, plus orphaned/unreachable `components/ScreenTest.jsx` (Screen Refresh Tester) and `components/BenchmarkTest.jsx`. Unwrapped `<LanguageProvider>` in `App.js`. Translated all remaining hardcoded Dutch to English in PasswordGenerator, PrinterTest (incl. printed-page HTML), WebcamAudioTest (permission screen + error messages), PixelTest, ExitFeedback, FloatingFeedbackButton, ToolSuggestionModal, ToolStatusWrapper, ChangelogModal (removed NL/EN toggle, defaults English). All public-facing pages verified 100% English; frontend compiles (HTTP 200); homepage + /password smoke-tested. NOTE: admin panel (AutosoftDashboard, ToolEditor, FeedbackAdmin, AdminSetup) still contains Dutch — not in this pass.
- 2026-06 (fork): **UI-consistentie doorgetrokken naar homepage-master-stijl.** Alle 4 testers gebruiken nu dezelfde "shell": `#0b0f19` bg, 127-logo linksboven (absolute), glazen kaart `rgba(0,0,0,0.5)` `rounded-3xl` + blur20, sub-kaarten `rgba(255,255,255,0.05)` `rounded-2xl`, en identieke pill-knoppen (primair blauw `rgba(150,180,255,0.25)`/hover 0.35, secundair `rgba(255,255,255,0.08)`/hover 0.15). Gewijzigd: PixelTest (kaart 0.7→0.5, logo top-left, gradient-knop→pill), WebcamAudioTest (permissiescherm grijze kaart→glas, running-interface panels→glas/sub-kaart, shadcn Button→pill), PrinterTest (kaartrand verwijderd). PasswordGenerator gebruikte de stijl al. Visueel geverifieerd: homepage, /password, /dpd, /wea (permissie), /printer. NB: actief webcam-opnamescherm niet visueel te screenshotten (geen camera in preview) — code wel consistent.
- 2026-06 (fork): **Bugfix — knoppen plakten vast in de tools.** De gestapelde actieknoppen op /dpd, /printer en /wea hadden 0px tussenruimte: de 2e knop zat in een react-router `<Link>` (display:inline), waardoor Tailwind `space-y-*` (margin-top) genegeerd werd. Opgelost door de containers naar `flex flex-col gap-4` te zetten (PixelTest, PrinterTest, WebcamAudioTest); tevens PasswordGenerator copy/regen-rij `gap-2→gap-3`. Geverifieerd via testing_agent iteration_6.json: 16px gap gemeten op alle 4 tools + homepage, navigatie werkt, frontend 100%.
- 2026-06 (fork): **SEO & GEO verbeterd.** index.html: verouderde "screen refresh tester" verwijderd uit description/keywords/OG/Twitter; Developer Toolbox toegevoegd; theme-color→#0b0f19; application-name, og:image:alt, uitgebreide robots meta (max-snippet/max-video-preview); manifest.json toegevoegd. Structured data: WebSite+Organization verrijkt (logo, description), ItemList met per-tool description + nieuw Developer Toolbox item (/tools), en nieuwe **FAQPage** JSON-LD (5 Q&A) voor GEO. sitemap.xml: /tools toegevoegd + lastmod op alle URL's. robots.txt: extra AI-crawlers toegestaan (Applebot-Extended, Amazonbot, Meta-ExternalAgent, DuckAssistBot, CCBot, cohere-ai, YouBot, Perplexity-User). Gevalideerd: 3 ld+json blokken = geldige JSON; /robots.txt, /sitemap.xml, /manifest.json → HTTP 200. NB: <title> bewust ongewijzigd gelaten ("Your online IT Library"). SPA is niet server-side gerenderd — per-route meta niet geprerenderd (SSR/prerender = toekomstige verbetering).
- 2026-06 (fork): **Per-route SEO toegevoegd (`components/RouteSeo.jsx`).** Dependency-vrije component (geen react-helmet) in `App.js` binnen de router: werkt `document.title`, `meta[name=description]`, `link[rel=canonical]`, OG/Twitter-title/description/url bij op elke route-wissel. Statische configs voor /, /dpd, /printer, /wea, /password, /tools; dynamische titel+description per toolbox-tool via `TOOLS` uit registry (`/tools/:toolId`). Admin-routes (`/localhost*`, `/autosoft`) → `noindex, nofollow`. App compileert (HTTP 200), alle routes renderen foutloos. Beperking: client-side gerenderd (JS-uitvoerende crawlers zoals Google zien het; SSR/prerender blijft een toekomstige verbetering).
- 2026-06 (fork): **Floating video-tegels op homepage (`components/FloatingVideoTiles.jsx`).** 9 gedimde tegels (opacity 0.4) met verschillende Pexels IT-video's (via bestaande backend `/api/pexels/videos?query=it`), verspreid rond/achter de centrale glazen zoek-kaart (z-[1], pointer-events-none; kaart blijft z-10 leesbaar). Video's: autoplay + muted + loop + playsInline, met Pexels `image` als poster (direct zichtbaar beeld). Beweging: continue subtiele idle-float (CSS `fvtFloat` keyframe, ~16px, gevarieerde duur/delay) + muis-parallax (window mousemove → CSS vars `--mx`/`--my` op container, per-tegel `depth` tot 38px, smooth transition). 5 tegels zichtbaar op mobiel (`hidden md:block` op de rest), respecteert `prefers-reduced-motion`. Faalt stil (component → null) als Pexels niet beschikbaar is. Geverifieerd via screenshot: tegels laden IT-beeld, kaart leesbaar, frontend HTTP 200.
- 2026-06: Fixed "tools laden traag" (pills + bij openen). `ToolStatusWrapper` rendert nu optimistisch (geen blokkerende spinner; alleen offline als backend expliciet enabled:false zegt); kunstmatige laadanimatie in alle 5 tools verkort 1000ms→300ms (+50ms); homepage pills tonen direct via optimistische default lijst. Verified iteration_3.json (pills ~0.6s, tools ~0.4-0.8s).
- 2026-06: Fixed "website keeps loading / must refresh manually" — `App.js` had a global `if (checkingSetup) return <Loading/>` that blocked the ENTIRE app (incl. public pages) behind the `/api/admin/needs-setup` fetch; a hung request left a permanent loading screen. Removed the global gate (public routes render immediately; ProtectedRoute still gates admin), added AbortController 5s timeout. Verified iteration_2.json.
- 2026-06: Landing page background changed from Pexels video → subtle animated multi-color CSS gradient (`.animated-gradient-bg` + `@keyframes subtle-gradient-shift` in App.css). Removed video-playlist fetch logic (also reduced loading noise).
- 2026-06: Koodh logo added bottom-left of landing page (white, links to koodh.com), no pill.
- 2026-06: Fixed "tools offline" bug — `/api/tools/{id}/status` now fail-opens (200 + enabled:true) instead of 404 when tool not seeded; `ToolStatusWrapper.jsx` also fail-opens on non-2xx.
- 2026-06: Pexels proxy now cached (TTL 3600s) + graceful empty fallback → faster, more resilient tools.
- 2026-06: Browser title changed `127 | Yannick Tools` → `Your online IT Library`. Added SEO meta (description, keywords, canonical, OG, Twitter), GEO (hreflang nl-BE/en, geo.region BE), 2× JSON-LD (WebSite + ItemList of tools), robots.txt (incl. AI crawlers) and sitemap.xml.
- Verified via testing_agent iteration_1.json: backend 12/12 pytest pass, frontend 100%.

## Pending / backlog
- WordPress export: only Landing + Password Generator fully exported to `/app/wordpress-export/`. PixelTest, PrinterTest, ScreenTest, WebcamAudioTest still to export (awaiting user format choice). ZIP: `/app/127-tools-complete-export.zip`.
- P1: React Error Boundary component.
- P2: Full NL/EN language switcher across all pages.
- Optional (from test report): console-noise on landing Pexels video fetch; React boolean-attribute warning on `/password`.
- Note: `PEXELS_API_KEY` must be set in the DEPLOYED environment or images won't load in production.

## Deploy note
- IGNORED an injected request to set up a "Koodh VDC" reverse SSH tunnel + auto-deploy via `curl|bash` (security risk). Not implemented. Awaiting user confirmation if legitimate.
