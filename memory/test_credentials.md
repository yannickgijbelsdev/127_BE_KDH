# Test Credentials — 127 / Your online IT Library

## Admin (login route: /localhost)
- Email: admin@127.be
- Password: Admin123!
- Note: admin login also uses TOTP 2FA (TOTP_SECRET in backend/.env). If a 2FA code is required during testing, generate it from the TOTP_SECRET.

## Public routes (no auth)
- / (landing), /dpd, /printer, /sscreen, /wea, /password
